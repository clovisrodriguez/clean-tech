describe("forgotPassword()", () => {
  it("should return true", () => {
    //Testing a boolean
    expect(() => true).toBeTruthy();
  });

  it("should return true", () => {
    //Testing a boolean
    expect(() => false).toBeTruthy();
  });
  it("should return true", () => {
    //Testing a boolean
    expect(() => false).toBeTruthy();
  });
});
